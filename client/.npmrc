# Cloudflare Pages installs with NODE_ENV=production, which omits
# devDependencies by default. Include them so the build tooling (vite,
# @vitejs/plugin-react, tailwind) is installed.
include=dev
# Ensure esbuild's postinstall runs to install its native binary.
ignore-scripts=false
